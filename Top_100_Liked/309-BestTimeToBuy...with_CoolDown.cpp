#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int maxProfit(vector<int>& prices) {
        if (prices.size() == 0) return 0;
        vector<int> buy(prices.size());
        vector<int> sell(prices.size());
        vector<int> rest(prices.size());
        
    }
};